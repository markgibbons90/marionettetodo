{{#if collectionLength}}
<button class="btn btn-link" data-action="toggle-completed-tasks">
    {{#if displayTaskList}}
    <span class="octicon octicon-chevron-down"></span>
    {{else}}
    <span class="octicon octicon-chevron-right"></span>
    {{/if}}
</button>
{{/if}}
<div id="completed-task-list-container" {{#unless displayTaskList}}class="hidden"{{/unless}}></div>
